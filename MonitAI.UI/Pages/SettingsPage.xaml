<Page x:Class="MonitAI.UI.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="SettingsPage"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="システム設定" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />

            <ui:CardControl Header="Gemini API設定" Icon="{ui:SymbolIcon Key24}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Gemini API Key:" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="ApiKeyBox" Margin="0,0,0,10" />
                    <Button Content="APIキーを保存" Click="SaveApiKey_Click" />
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl Header="システム診断" Icon="{ui:SymbolIcon Stethoscope24}">
                <StackPanel>
                    <TextBlock Text="Gemini CLI (Python環境) の動作確認を行います。" Foreground="Gray" Margin="0,0,0,10"/>
                    <Button Content="CLI接続テストを実行" Click="TestCli_Click" Margin="0,0,0,10"/>
                    <TextBlock x:Name="CliStatusText" Text="未確認" Foreground="Gray" TextWrapping="Wrap"/>
                </StackPanel>
            </ui:CardControl>
        </StackPanel>
    </Grid>
</Page>